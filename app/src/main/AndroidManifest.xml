<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FieldService">
        <activity
            android:name=".presentation.login.LoginActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.FieldService"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".presentation.home.HomeActivity"
            android:exported="false"
            android:label="@string/app_name"
            android:theme="@style/Theme.FieldService" />
        <activity
            android:name=".presentation.tickets.TicketsActivity"
            android:exported="false"
            android:label="@string/tickets_title"
            android:parentActivityName=".presentation.home.HomeActivity"
            android:theme="@style/Theme.FieldService" />
        <activity
            android:name=".presentation.ticketdetails.TicketDetailsActivity"
            android:exported="false"
            android:label="@string/detail_title"
            android:theme="@style/Theme.FieldService" />
        <activity
            android:name=".presentation.profile.ProfileActivity"
            android:exported="false"
            android:label="@string/profile_title"
            android:parentActivityName=".presentation.home.HomeActivity"
            android:theme="@style/Theme.FieldService" />
    </application>

</manifest>