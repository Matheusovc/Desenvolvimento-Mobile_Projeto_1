<?xml version="1.0" encoding="utf-8"?>
<!-- Um passo da timeline de status do atendimento: linha (topo) + ponto + linha (base) + rótulo.
     Cores e visibilidade das linhas são definidas por código conforme o progresso. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="48dp"
    android:orientation="horizontal">

    <LinearLayout
        android:layout_width="24dp"
        android:layout_height="match_parent"
        android:gravity="center_horizontal"
        android:orientation="vertical">

        <View
            android:id="@+id/lineTop"
            android:layout_width="2dp"
            android:layout_height="0dp"
            android:layout_weight="1"
            android:background="@color/color_outline" />

        <ImageView
            android:id="@+id/dot"
            android:layout_width="14dp"
            android:layout_height="14dp"
            android:contentDescription="@null"
            android:src="@drawable/timeline_dot" />

        <View
            android:id="@+id/lineBottom"
            android:layout_width="2dp"
            android:layout_height="0dp"
            android:layout_weight="1"
            android:background="@color/color_outline" />

    </LinearLayout>

    <TextView
        android:id="@+id/stepLabel"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_marginStart="@dimen/space_sm"
        android:gravity="center_vertical"
        android:textAppearance="@style/TextAppearance.FieldService.Body" />

</LinearLayout>
